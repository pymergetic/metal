[package]
name = "sample_greeter"
version = "0.1.0"
edition = "2021"

[lib]
path = "../__init__.rs"
crate-type = ["cdylib"]

[profile.release]
panic = "abort"
lto = true
opt-level = "z"
codegen-units = 1
strip = true
