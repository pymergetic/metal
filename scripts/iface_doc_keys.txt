# doc_key overrides for scripts/gen_iface_syms.py (docs/DOC_IFACE_PLAN.md
# Part II-G) — one "<module> <native_name> <doc_key>" line, whitespace
# separated, '#' comments allowed. <module> + <native_name> must match a
# NativeSymbol row's own module (PM_METAL_*_WASI_MODULE) + "name" exactly;
# <doc_key> is a pointer into util/doc.c's catalog (Part I), "<kind>:<key>",
# never pasted summary/sig/body text (see util/iface.h's own file header —
# "no auto doc_key for all natives" is a deliberate non-goal, this file is
# an explicit, reviewed allowlist, not a scrape).
#
# fs.c's own wasm-imported native for open() is the async variant (fs.c
# has no plain sync "pm_metal_fs_open" NativeSymbol row — see fs.h); its
# doc-catalog entry lives on the Python face instead (fs_py_bind.c's
# PY_BIND_DOC), so the doc_key below points at that face's own dotted
# bind path.
pymergetic.metal.fs pm_metal_fs_open_async py:pymergetic.metal.fs.open
