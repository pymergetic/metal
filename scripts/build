#!/usr/bin/env bash
# Build interface — EFI freestanding Metal + guest artifacts.
# Usage: scripts/build <target> [all]
#        scripts/build all
set -euo pipefail

ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
PORT="${ROOT}/scripts/build.d/port"
GUEST="${ROOT}/scripts/build.d/guest"
# shellcheck disable=SC1091
source "${ROOT}/scripts/lib/run-steps.sh"

usage() {
	cat >&2 <<'EOF'
usage: scripts/build <target> [all]
       scripts/build all

  efi       — freestanding UEFI Metal (scaffold → metal.efi)
  mod       — guest wasm tests
  cpython   — python.wasm
  all       — mod + cpython + efi

Examples:
  scripts/build all
  scripts/build efi
  scripts/build mod
EOF
	exit 1
}

run() {
	local script="$1"
	shift
	if [[ ! -f "${script}" ]]; then
		echo "missing ${script}" >&2
		exit 1
	fi
	exec "${script}" "$@"
}

[[ $# -ge 1 ]] || usage
plat="$1"
shift

case "${plat}" in
-h | --help | help) usage ;;
all)
	[[ $# -eq 0 ]] || usage
	pm_run_steps "${ROOT}/scripts/build" "mod" "cpython" "efi"
	exit $?
	;;
efi | mod | cpython)
	if [[ $# -ge 1 ]]; then
		[[ "$1" == "all" && $# -eq 1 ]] || usage
	fi
	;;
*) usage ;;
esac

case "${plat}" in
efi) run "${PORT}/efi/default.sh" ;;
mod) run "${GUEST}/mod.sh" ;;
cpython) run "${GUEST}/cpython.sh" ;;
*) usage ;;
esac
