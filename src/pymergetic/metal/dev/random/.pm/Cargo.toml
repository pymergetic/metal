[package]
name = "pymergetic_metal_dev_random"
version = "0.1.0"
edition = "2021"

[lib]
path = "../_impl/__init__.rs"
crate-type = ["staticlib", "rlib"]

[dependencies]
pymergetic_metal_rt = { path = "../../../rt/.pm" }
pymergetic_metal_dt = { path = "../../../dt/.pm" }

[profile.release]
panic = "abort"
lto = true
codegen-units = 1
