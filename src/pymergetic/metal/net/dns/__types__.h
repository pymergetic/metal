/* pymergetic.metal.net.dns — A records on net.ip UDP. */
#ifndef PYMERGETIC_METAL_NET_DNS_TYPES_H
#define PYMERGETIC_METAL_NET_DNS_TYPES_H

#include "pymergetic/util/mem/__types__.h"

#include <stdint.h>

/* pm_mod_value_t — an IPv4 address, big-endian, as it crosses a face: a
 * number the caller means rather than the length of the name beside it. The
 * distinction is the border's, which is why the type is the border's and not
 * this card's; spelt uint32_t, add() was reachable from C and refused from
 * Python. */
#include "pymergetic/wasmmod/guest.h"

#endif /* PYMERGETIC_METAL_NET_DNS_TYPES_H */
