[package]
name = "pymergetic_metal_net_http_microdot"
version = "0.1.0"
edition = "2021"

[lib]
path = "../__init__.rs"
crate-type = ["staticlib", "rlib"]

[dependencies]
pymergetic_metal_rt = { path = "../../../../rt/.pm" }
# Doc-browser pages call through C ABI (reg/fs); path deps keep link order.

[profile.release]
panic = "abort"
lto = true
codegen-units = 1
