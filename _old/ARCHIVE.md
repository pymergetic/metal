# Archived Metal product

Former package-root product tree, moved with the same relative layout
(`src/`, `include/`, `mods/`, `scripts/build|run|…`, product docs, …).

**Live firmware** is [`../`](../) via `./forge-cli` at the package
root. Design docs restored to live [`../docs/`](../docs/) (EFI, IO, memory,
Kconfig, mods, …); product-only papers (µPy, mount, WASI, …) stay here under
[`docs/`](docs/). See [`README.md`](README.md) for the historical overview.
