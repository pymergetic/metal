# SPDX-License-Identifier: Apache-2.0
# Umbrella placeholder — EFI product builds via EDK2 (./scripts/build efi).
# See docs/EFI.md, src/efi/README.md.

cmake_minimum_required(VERSION 3.20)
project(pymergetic-metal C)

message(STATUS "pymergetic-metal: use ./scripts/build efi (EDK2), not this CMake tree")
