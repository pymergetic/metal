#!/usr/bin/env bash
# Deprecated: product Python is staged loose (scripts/lib/stage-py-trees.sh).
# Kept only so old docs/links fail loudly instead of rebuilding zips.
set -euo pipefail
echo "pack_zips: obsolete — use scripts/lib/stage-py-trees.sh (loose /mods import layout)" >&2
exit 1
