# Quarantine (`_tmp`)

Old parallel “muscle” trees and other live-path junk move **here** while the
product tree is consolidated to wasmmod style (`include/` + `src/` hybrid).

**Not linked by the port.** Do not add new product APIs under `_tmp/`.
See [`docs/HYBRID.md`](../docs/HYBRID.md).
