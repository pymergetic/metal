# Build outputs — see docs/SOURCETREE.md
/build/

# Legacy layout (pre-src/ migration; may still exist under backup/)
host/**/build/
host/**/images/
runtime/**/build/
runtime/**/images/
runtime/**/bench/build/
mods/build/

# Backup tries — west clones and local build output (source snapshots stay tracked)
backup/**/external/
backup/**/build/
backup/**/images/
backup/**/generated/

# Toolchain (local download)
.tools/

# West workspace clones
/external/
/modules/
/bootloader/
/tools/

# Git submodule checkout (.gitmodules; west uses external/)
/third_party/

# West local state (keep config)
.west/*
!.west/config

# IDE / tooling
.vscode/
.idea/
.cache/
.venv/
compile_commands.json
**/compile_commands.json
cmake-build-*/

# Zephyr test runner
twister-out/
**/twister-out/

# Generated embeds (wasm → C arrays)
src/zephyr/generated/
**/host/zephyr/generated/
