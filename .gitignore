# Build outputs — see docs/SOURCETREE.md
/build/

# Legacy layout leftovers (pre-src/ migration)
host/**/build/
host/**/images/
runtime/**/build/
runtime/**/images/
runtime/**/bench/build/
mods/build/

# Toolchain (local download)
.tools/

# West workspace clones
/external/
/modules/
/bootloader/
/tools/

# Git submodule checkout (.gitmodules; west uses external/)
/third_party/

# West local state (keep config)
.west/*
!.west/config

# IDE / tooling
.vscode/
.idea/
.cache/
.venv/
compile_commands.json
**/compile_commands.json
cmake-build-*/

# Generated from .clangd.template by scripts/setup-ide.sh (checkout-specific
# absolute paths — see that script and .clangd.template's own comment)
/.clangd

# Zephyr test runner
twister-out/
**/twister-out/

# Generated embeds (wasm → C arrays)
src/zephyr/generated/
**/host/zephyr/generated/

# setup-nuttx.sh local link for upstream wamr.mk layouts
/src/nuttx/wamr
