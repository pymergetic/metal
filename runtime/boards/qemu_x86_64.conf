# Real metal (qemu_x86_64): x86 MMU virtual map cap (not machine RAM)
CONFIG_KERNEL_VM_SIZE=0x40000000

# Runtime RAM discovery via multiboot E820 (not DT pm_machine_ram.dtsi)
CONFIG_MULTIBOOT_MEMMAP=y
