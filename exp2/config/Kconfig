# exp2 root Kconfig — menus mirror pymergetic.metal modules.
# See docs/definitions/fs.md and docs/KCONFIG.md (exp2 section).

mainmenu "Metal exp2 Configuration"

comment "Build / run (exp2/scripts/build defaults)"
source "Kconfig.build"

comment "Runtime modules (pymergetic.metal.*)"
source "metal/Kconfig"
