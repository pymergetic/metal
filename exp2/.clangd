# Prefer package-root .clangd (from .clangd.template via ./scripts/setup ide).
# Package-root .clangd uses repo-relative -I only (no checkout-absolute paths).
# Do not add -Isrc here — that short path collides with product includes.
---
Diagnostics:
  UnusedIncludes: None
  MissingIncludes: None
