[package]
name = "pymergetic_metal_fs_embed"
version = "0.1.0"
edition = "2021"

[lib]
path = "../__init__.rs"
crate-type = ["staticlib", "rlib"]

[dependencies]
pymergetic_metal_rt = { path = "../../../rt/.pm" }

[profile.release]
panic = "abort"
lto = true
codegen-units = 1
