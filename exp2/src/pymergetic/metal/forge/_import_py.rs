//! Import: Python source -> Catalog (stub).

use crate::_catalog::Catalog;

/// Parse Python module text into a catalog. Not implemented yet.
pub fn import(_text: &str) -> Result<Catalog, ()> {
    Err(())
}
