# Root Kconfig — menus mirror pymergetic.metal modules.
# See docs/definitions/fs.md and docs/KCONFIG.md.

mainmenu "Metal Configuration"

comment "Build / run (./forge-cli build defaults)"
source "Kconfig.build"

comment "Runtime modules (pymergetic.metal.*)"
source "metal/Kconfig"
