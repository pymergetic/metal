# frozen_ns

Empty `pymergetic` / `pymergetic.metal` package markers for **MicroPython freeze only**.

Host and metal-cdn keep PEP 420 namespaces (no `__init__.py` on those parents).
Do not copy these into `src/pymergetic/`.
